class Estado {
    enter(estadoAnterior){
        //no se hace nada, ya que este es la clase abstracta de los estados
    }
    update(dt) {
        //no se hace nada, ya que este es la clase abstracta de los estados
    }
    exit(estadoSiguiente) {
        //no se hace nada, ya que este es la clase abstracta de los estados
    }
}